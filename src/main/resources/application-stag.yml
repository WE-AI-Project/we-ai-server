spring:
  jpa:
    hibernate:
      ddl-auto: validate
  docker:
    compose:
      enabled: false

logging:
  level:
    root: info
    com.weai.server: info
    org.springframework.security: info
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: warn

server:
  forward-headers-strategy: framework

app:
  web:
    base-url: ${APP_BASE_URL:https://api-stag.example.com}
    frontend-base-url: ${APP_FRONTEND_BASE_URL:https://stag.example.com}
    external-domains:
      api-domain: ${APP_API_DOMAIN:api-stag.example.com}
      frontend-domain: ${APP_FRONTEND_DOMAIN:stag.example.com}
      storage-console-domain: ${APP_STORAGE_CONSOLE_DOMAIN:minio-stag.example.com}
    cors:
      allowed-origins:
        - ${APP_FRONTEND_BASE_URL:https://stag.example.com}
      allowed-methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      allowed-headers:
        - Authorization
        - Content-Type
        - X-Requested-With
        - X-Request-Id
      exposed-headers:
        - X-Request-Id
      allow-credentials: true
      max-age: 30m
